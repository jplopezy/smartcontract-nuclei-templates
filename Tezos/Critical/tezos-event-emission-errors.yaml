id: tezos-event-emission-errors

info:
  name: Tezos Event Emission Errors Vulnerability
  author: Juan Pablo Lopez Yacubian
  severity: Critical
  description: A contract fails to emit proper events, reducing auditability and transparency.
  tags: tezos,critical

file:
  - extensions:
      - tz

    matchers:
      - type: regex
        regex:
          - "\bfunction\b"
          - "\binvoke\b"
          - "\blog\b"
        condition: and
